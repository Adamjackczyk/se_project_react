// src/contexts/CurrentUserContext.js

import React from "react";

/**
 * CurrentUserContext
 *
 * Provides the current user data across the application.
 */
const CurrentUserContext = React.createContext(null);

export default CurrentUserContext;
